import 'package:flutter/material.dart';

// A primary color definition
const primaryColor = Color(0xFF7956FF);
const primaryLightColor = Color(0xFFFFECDF);
const primaryGradietColor = LinearGradient(colors: [
  Color(0xFFFFA53E),
  Color(0xFFFF7643),
]);

// A secondary color definition
const secondaryColor = Color(0xFF979797);
const tertiaryColor = Color(0xFFFF55A7);
const additionalColor = Color(0xFFB4FF01);
const textColor = Color(0xFF757575);

// animationDuration time definition
const animationDuration =
    Duration(milliseconds: 200); // best practicenya 200-400

//default constant value for padding size
const defaultPadding = 20.0;
